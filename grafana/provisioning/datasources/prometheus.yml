# Grafana 자동 데이터소스 등록 — compose 네트워크의 prometheus 컨테이너를 가리킴.
apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus          # 대시보드 패널이 이 uid 로 참조
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
