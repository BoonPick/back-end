apiVersion: v1
kind: ConfigMap
metadata:
  name: boonpick-config
  namespace: boonpick
data:
  WORKERS: "4"
  DB_POOL_SIZE: "16"
  SMTP_HOST: "smtp.gmail.com"
  SMTP_PORT: "587"
  MAIL_FROM_NAME: "BoonPick"
